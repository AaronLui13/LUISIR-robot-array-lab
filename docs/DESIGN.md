# 機械人倉庫設計紀錄

## 已實作的操作主線

桌面採用貨架、指令工作區、任務工作單三欄，下方顯示執行紀錄。手機依序排列，課程地圖可展開。倉庫顯示零起始索引、目前讀取／寫入位置、走訪次數和選取標記。座標變換保留原表，並在新表追蹤目的位置；C 固定橙色。

每關先輸入預測、點選要求的位置，再鎖定預測。預測不正確不阻止學習；正確預測可獲額外標示。學生可修改每張卡的參數、排序、縮排或加入新卡。任務骨架預填結構但留一個關鍵 `?`，自由編排從空白開始。

縮排定義重複／如果的巢狀結構。支援讀取、寫入、輸出、重複、檢查、設定／加總／暫存、條件、位置回傳、停止、新表及目的位置寫入。`rows` 和 `cols` 是由資料推導的只讀尺寸；整除 `//`、餘數 `%`、比較與 `and`／`or` 可在欄位內組合。布林運算支援短路求值。預先產生不可互相影響的執行快照，逐步呈現或每 360ms 播放一步；撤銷還原貨架、變數、輸出與走訪紀錄。

遇到未初始化變數、無效算式、索引越界、錯誤縮排或過多步驟，記錄具體原因並可立即重試。執行器不使用 eval，不執行學生 JavaScript；每次最多 4000 步，新表每個維度最多 10 格。Python 面板是語法對照，`process` 是遊戲檢查標記，`return` 需位於函數內。

## 關卡

| 課題 | 關卡 | 已實作內容 |
|---|---|---|
| 教學 | 00 | 依次點選及讀取 (0,2)、(2,0)，輸出 4、0，分清位置與數量 |
| 一 | 01–02 | 正反向地址、補貨到 5、重新讀取及概念題 |
| 二 | 03–04 | 逐行遍歷、每格一次、全表與每行空位計數 |
| 三 | 05–06 | 搜尋 3、搜尋 2 的第一個與全部位置、搜尋不存在的 9 |
| 四 | 07–08 | 逐行一維編號、整除餘數還原、每行 4 格與 5 格互測 |
| 五 | 09–10 | 計算候選位置、邊界／佔用檢查、合法移動／撞貨／越界、覆寫示範與暫存交換 |
| 六 | 11–12 | 逐列、蛇形走訪及指定 (1,0) 的蛇形編號挑戰 |
| 七 | 13–14 | 左右／上下翻轉、轉置／順時針 90°、2×3 轉為 3×2、追蹤 C |
| 八 | 15–16 | 區域六格加總 10、不重疊 2×2 合併、滑動視窗最大值、3×5 的捨去邊緣／補 0 政策 |
| 綜合 | 17 | 搜尋第一空位，輸出位置與一維索引，補入 6，使用更新後資料求最大 2×2 區域 |

前期共用資料為 `[[2,0,4,1],[1,3,0,2],[0,2,1,0]]`。搬運、字母及區塊題使用明確的專用資料。17 的 A、B 工作單都是 3×5，必須由同一方法通過；平手額外測試確保保留最早位置。

## 判定與回饋

- 純函數以獨立計算的目標核對結果、來源保持不變、指定處理次數及走訪次序。
- 搜尋第一個要在找到後停止，不得繼續檢查其他格。找不到要輸出「找不到」，每次執行重新初始化。
- 遍歷、計數、搜尋、索引、交換、區域及區塊題另用數值或尺寸變更的資料檢查方法；17 也測試同值情況。
- 提示逐步指出觀察現象、檢查範圍／縮排／初始化，最後顯示一張關鍵卡的參數，並保留其餘操作給學生。
- 正確執行後答一條概念題。含變式的任務需要完成各變式才顯示完整任務章；07、08、17 通過自動雙資料驗證即記錄兩組通過。
- 任務章與預測獎勵只寫入 `localStorage`。儲存不可用時遊戲仍可進行；沒有排行榜或網路資料回傳。

## 課堂使用

八課開始前玩 00–03。每課以一個短任務引出問題，講解後重試並查看 Python，結課時完成變式。八課後以 17 檢驗獨立整合。教師可直接跳到任何關卡，不受前關完成度限制。每個短任務預期少於十分鐘，綜合題可按課堂安排延長。

## 範圍說明

目前自由編寫指令參數與算式，並提供空白指令工作區；沒有任意 Python 編輯器、Python 執行沙箱或教師集中管理平台。音效未加入，網站保持靜音。沒有學生帳戶、分析工具、外部字型或第三方圖片。
