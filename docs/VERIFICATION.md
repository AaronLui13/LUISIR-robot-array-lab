# 驗證紀錄

## 規則測試

57 項 Node 測試覆蓋所有 18 關及變式，另包含索引與算式錯誤、短路求值、縮排、步數上限、快照隔離、找不到時清空輸出、錯誤初始化、覆寫損失、非法搬運不修改資料、尺寸轉換、走訪次序、非方形變換、區域邊界政策、第二組資料及同值保留第一個。`npm test` 包含靜態建置，`npm run lint` 使用 ESLint。

## 瀏覽器檢查

- 桌面 1440×1000：三欄排列、初始關卡、預測、無效 `?` 的錯誤回饋、修改後重試、逐步執行、撤銷、結果與概念題任務章。
- 手機 390×844：初始貨架、課程地圖、02 補貨、逐步執行、撤銷還原數量及核對結果；頁面沒有整體水平溢出。
- 手機進階關卡：14 順時針旋轉得到 `[[D,A],[E,B],[F,C]]`，C 在 (2,1)；Python 對照在執行後保持展開。
- 鍵盤：以 Enter 執行一步後焦點留在執行按鈕；修改算式時保留文字輸入焦點。
- 17 終極工作單：實際播放 155 步，輸出 `(0,1) · 1 · 13 · (0,1)`，通過另一組資料與平手測試，完成概念題取得任務章。
- 瀏覽器執行期間未觀察到 JavaScript console error。GitHub Pages 的實際公開網址於交付時另作存取檢查。

## 限制

自動規則測試不等於每個裝置的完整無障礙驗證。未實測實體平板、螢幕閱讀器或教室投影機。網站沒有聲音，CSS 尊重減少動態效果的系統設定。

## 2026-09-19 更新驗證

- 新增六組回歸測試：Python 連鎖比較與短路、負數除法／餘數、動態索引答案與兩種慣例、關鍵填空、偽代碼區塊與卡片對照、草稿復原及損壞／不可用儲存處理。
- 瀏覽器實測 07 每行 5 格：填 11、補 r * cols + c，完成 34 步及概念題後取得「預測準確」；完成條件為 0–14。
- 重新開頁仍在 07 的 5 格變式，保留公式與鎖定預測 11，執行重新由第 0 步開始。
- 09 向左越界以 `0 <= nr < rows and 0 <= nc < cols` 執行 5 步，條件不成立、沒有越界讀取，核對通過。
- 14 轉置第 8 步：C 到 (2,0)，結果表列標籤 0／行標籤 2 同時高亮；Python 對應寫入行同步高亮。
- 390×844 視窗顯示寬度 390、文件寬度 390；工作區快照、卡片及執行列可同屏觀看。這不代表已完成實體觸控裝置或所有瀏覽器驗收。

## 遊戲動畫更新驗證

- 新增三組快照事件測試：區分讀取／掃描／輸出，寫入／複製保留來源目的值，越界不產生移動目標。動畫只讀取快照，不修改貨架或學生指令。
- 瀏覽器實測 14 轉置第 6 步：可看到 B 的飛行圖示及「0 → B」提示；第 8 步關閉動畫後沒有飛行或寫入特效，資料仍正確。
- 14 完成結果核對及概念題後顯示「變式完成」慶祝，與未完成全部變式的任務章狀態一致。
- 390×844 手機尺寸下，控制台與格子沒有水平溢出；點格可移動機械人。動畫關閉偏好在重新載入後保留。
- 裝置的 reduced-motion 分支以程式檢查確認；沒有更改實體裝置設定作測試。

## 逐步引導更新驗證

- 首頁先提供開始、接續及教師選關入口；關內依預測／補指令／執行／結果分步顯示。
- 實測 00：錯格提示、依序點選、4/0 預測、未填欄位提示、補行索引 0、四步執行、核對、答錯概念題後重試及前往 01。
- 重新載入顯示首頁，選接續後保留鎖定預測與已填欄位；執行從第 0 步開始。
- 390×844 預覽沒有水平溢出；第一關貨架與提示同屏，執行頁的貨架、快照及執行按鈕同屏。進階工具仍可展開帶註解的偽代碼。
- 新增三組流程測試，涵蓋必要選格、容許錯誤預測及下一個未完成變式／關卡；總計 60 項通過，ESLint 通過。未觀察到瀏覽器錯誤。
